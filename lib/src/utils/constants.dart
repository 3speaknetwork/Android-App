import 'package:flutter/cupertino.dart';

const kScreenHorizontalPaddingDigit = 15.0;
const kScreenVerticalPaddingDigit = 15.0;
const kScreenHorizontalPadding =
    EdgeInsets.symmetric(horizontal: kScreenHorizontalPaddingDigit);
const kScreenVerticalPadding =
    EdgeInsets.symmetric(vertical: kScreenVerticalPaddingDigit);
const kScreenPadding = EdgeInsets.symmetric(
    vertical: 15, horizontal: kScreenHorizontalPaddingDigit);
